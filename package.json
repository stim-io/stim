{
  "name": "stim",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build:client": "pnpm -C packages/client build",
    "build:renderer": "pnpm -C packages/client build && pnpm -C apps/renderer/vite build",
    "format:check": "pnpm exec prettier --check .",
    "generate:client": "pnpm -C packages/client generate",
    "guard": "pnpm run typecheck:renderer",
    "typecheck:client": "pnpm -C packages/client typecheck",
    "typecheck:renderer": "pnpm -C packages/client build && pnpm -C apps/renderer/vite typecheck"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "0.91.0",
    "prettier": "^3.6.2"
  }
}
